@if (_farms.Count == 0)
{
    return;
}

@foreach (var farm in _farms)
{
    <PrimaryButton OnClick="() => NavigateTo(farm.Theme, farm.PlayerName, farm.ProfileId)">
        @farm.Theme.GetWords @farm.PlayerName.ToDisplayPlayerName() @farm.ProfileId.GetWords
    </PrimaryButton>
}