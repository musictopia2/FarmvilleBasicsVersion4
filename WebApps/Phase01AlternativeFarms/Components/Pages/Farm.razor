@page "/farm/{Theme}/{Player}/{ProfileId}/{SlotText}"
@if (_farmContainer is null)
{
    return;
}

<CascadingValue Value="_farmContainer">
    <MainComponent
        Theme="@Theme"
        Player="@Player"
        ProfileId="@ProfileId"
        />
</CascadingValue>
